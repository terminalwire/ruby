#!/usr/bin/env ruby
require_relative "../config/environment.rb"

raise "This binary is only for use in a development environment" if Rails.env.production?

Terminalwire::Client.websocket("ws://127.0.0.1:3000/terminal")
